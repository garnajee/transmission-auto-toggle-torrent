# IMPORTANT: Use the container name of your Transmission instance as the host
# if it's on the same Docker network.
TR_IP=transmission-docker
# Transmission RPC port (default is 9091)
TR_PORT=9091
TR_USERNAME= # empty if no auth
TR_PASSWORD= # empty if no auth

# Check interval in seconds
CHECK_INTERVAL=60

# Comma-separated list of tracker URLs to monitor
# IMPORTANT: Enter the exact URL as it appears in Transmission
TARGET_TRACKERS="http://tracker.example.com"

